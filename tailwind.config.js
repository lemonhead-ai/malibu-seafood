module.exports = {
    content: [
      "./src/**/*.{js,jsx,ts,tsx}", // Adjust this if your files are in a different directory
    ],
    theme: {
      extend: {}, // You can add custom colors, fonts, etc. here if needed
    },
    plugins: [],
  };